import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import GymProAI from "./GymProAI";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GymProAI />
  </React.StrictMode>
);
